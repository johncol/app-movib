import React, { ReactElement } from 'react';

export const Features = (): ReactElement => {
  return (
    <div className="features">This is displayed to new users, describe what the site is for</div>
  );
};
